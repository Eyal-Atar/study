---
phase: 21
plan: 21-03
type: frontend
wave: 4
depends_on: ["21-04"]
autonomous: true
requirements: [ONB-04, ONB-05]
files_modified:
  - frontend/js/ui.js
  - frontend/js/app.js
  - index.html
must_haves:
  truths:
    - AI transition screen displays simulated microcopy progress steps for "Aha!" moment.
    - Simulated progress lasts 4-6 seconds to align with backend Auditor/Strategist processing.
    - If !onboarding_completed, user is redirected to onboarding.
  artifacts:
    - Enhanced LoadingAnimator in frontend/js/ui.js
  key_links:
    - frontend/js/app.js -> frontend/js/onboarding.js (onboarding gate)
---

# Plan 21-03: Transition Screen & Application Integration

Finalize the "Aha!" moment with a smart transition screen and clean up the legacy UI.

<task>
<name>AI Progress Transition UI/JS</name>
<files>
- frontend/js/ui.js
- index.html
</files>
<action>
Enhance `LoadingAnimator` in `ui.js` to support step-based progress updates. Implement a full-screen transition UI in `index.html`. 
Logic: Simulated progress steps (4-6 seconds) will cycle through:
- "Extracting syllabus constraints..."
- "Mapping past exams to timeline..."
- "Calculating optimal time blocks..."
- "Finalizing your roadmap."
The sequence ensures the user "feels" the AI complexity while backend processing completes.
</action>
<verify>
Manual check: Transition screen shows correctly with timed microcopy transitions.
</verify>
<done>
Simulated AI progress transition screen implemented.
</done>
</task>

<task>
<name>Onboarding Gate (Routing)</name>
<files>
- frontend/js/app.js
</files>
<action>
Implement the onboarding gate in `app.js`. Redirect to the onboarding wizard if `onboarding_completed` is false in the user profile. Show the dashboard only if true.
</action>
<verify>
Test redirection: Log in as new user -> onboarding; complete onboarding -> dashboard.
</verify>
<done>
Onboarding gate enforced in application routing.
</done>
</task>

<task>
<name>Legacy UI Cleanup</name>
<files>
- index.html
- frontend/js/calendar.js
- frontend/js/ui.js
</files>
<action>
Remove all legacy "Regenerate" UI elements from the Dashboard, Settings, and Calendar views. Ensure no "empty calendar" states appear after onboarding.
</action>
<verify>
Manual audit of dashboard and calendar to ensure legacy buttons are gone.
</verify>
<done>
Legacy UI elements removed.
</done>
</task>
