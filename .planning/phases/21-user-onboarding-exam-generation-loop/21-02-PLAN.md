---
phase: 21
plan: 21-02
type: frontend
wave: 2
depends_on: ["21-01"]
autonomous: false
requirements: [ONB-01, ONB-02]
files_modified:
  - frontend/js/onboarding.js
  - index.html
  - frontend/css/styles.css
must_haves:
  truths:
    - Onboarding state is persisted in sf_onboarding_draft local storage.
    - Profiling screen uses preset chips for Study Hours (Morning, Afternoon, Night) and Buffer Days (0, 1, 2).
  artifacts:
    - frontend/js/onboarding.js (state machine)
    - Updated index.html with profiling screen
  key_links:
    - index.html -> frontend/js/onboarding.js (event listeners)
---

# Plan 21-02: Onboarding Wizard (Foundation & Profiling)

Build the "one screen, one focus" onboarding experience foundation with state management and the profiling questionnaire.

<task>
<name>Onboarding State Manager (JS)</name>
<files>
- frontend/js/onboarding.js
</files>
<action>
Create `onboarding.js` to manage the multi-step wizard state machine. Implement `saveDraft()` and `loadDraft()` using `localStorage` (`sf_onboarding_draft`).
</action>
<verify>
Check if `localStorage` is correctly populated and hydrated on refresh.
</verify>
<done>
Onboarding state manager exists and persists to localStorage.
</done>
</task>

<task>
<name>Profiling UI & Navigation</name>
<files>
- index.html
- frontend/css/styles.css
- frontend/js/onboarding.js
</files>
<action>
Add the onboarding screen structure and the profiling questionnaire step (step-0) to `index.html`. Add styling for chips/buttons. Wire the step navigation logic in `onboarding.js`.
</action>
<verify>
Manual UI check: Step-0 (Profiling) is visible and correctly styled.
</verify>
<done>
Profiling UI exists and is integrated into the wizard.
</done>
</task>

<task>
<name>Profiling Logic (Chips & Persistence)</name>
<files>
- frontend/js/onboarding.js
</files>
<action>
Implement multi-select for Study Hours and single-select for Buffer Days chips. Ensure the selection is correctly updated in the draft state.
</action>
<verify>
Verify that chip selections are reflected in the `onboarding_draft` state.
</verify>
<done>
Profiling selection logic works as intended.
</done>
</task>
