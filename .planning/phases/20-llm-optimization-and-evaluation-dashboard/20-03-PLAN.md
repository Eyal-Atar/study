---
phase: 20-llm-optimization-and-evaluation-dashboard
plan: 03
type: execute
wave: 3
depends_on: [20-02]
files_modified: [backend/eval/dashboard.py, backend/eval/judge_logic.py, backend/eval/prompts/judge_system.txt]
autonomous: true
requirements: [EVAL-04]

must_haves:
  truths:
    - "Judge model evaluates logical correctness of model output"
    - "Structural scoring (Pass/Fail) is displayed for each model"
    - "Judge provides a critique and a 'Compensating Prompt' snippet for failures"
  artifacts:
    - path: "backend/eval/judge_logic.py"
      provides: "Automated evaluation and critique logic"
    - path: "backend/eval/prompts/judge_system.txt"
      provides: "System instructions for the Judge model"
  key_links:
    - from: "backend/eval/dashboard.py"
      to: "backend/eval/judge_logic.py"
      via: "Post-execution validation step"
---

<objective>
Implement an automated "Judge" model and dual-layer scoring system for logical validation.
This plan focuses on using a powerful model to analyze the output of cheaper models, identifying logical errors (overlaps, constraint violations), and automatically suggesting prompt improvements.
</objective>

<execution_context>
@/Users/eyalatar/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/eyalatar/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-llm-optimization-and-evaluation-dashboard/20-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Judge logic and dual-layer scoring</name>
  <files>backend/eval/dashboard.py, backend/eval/judge_logic.py, backend/eval/prompts/judge_system.txt</files>
  <action>
    Create a `judge_logic.py` and integrate it into the dashboard:
    - Judge System Prompt: Create `judge_system.txt` with instructions for logical validation:
      - Overlapping tasks detection.
      - Adherence to mandatory windows (Lectures, Gym).
      - Respect for sleep constraints.
    - Structural Pass/Fail: Implement basic schema validation for JSON outputs.
    - Logical Score (1-10): Call the Judge model (e.g., gpt-4o) with the scenario, the model output, and the judge system prompt to assign a quality score.
    - Dashboard Integration: Display the Structural Pass/Fail and Logical Score prominently for both Current and Challenger models.
  </action>
  <verify>
    Verify Judge model receives the correct context and returns a score and critique.
    Verify Pass/Fail status is correctly determined for malformed JSON.
  </verify>
  <done>
    Dashboard displays structural and logical scores for all evaluated outputs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Automated Compensating Prompt generation</name>
  <files>backend/eval/dashboard.py, backend/eval/judge_logic.py</files>
  <action>
    Enhance the Judge logic to provide actionable feedback:
    - Critique: Include the judge's concise critique of detected errors in the UI.
    - Compensating Prompt: Prompt the Judge to generate a specific instruction snippet ("Compensating Prompt") that can be added to the Challenger's System Prompt to address the specific logical failure.
    - Dashboard UI: Show the Compensating Prompt snippet in a highlighted "Actionable Feedback" box with a "Copy Snippet" button.
  </action>
  <verify>
    Verify the Judge's compensating prompt is relevant to the error (e.g., if tasks overlap, the snippet says "Ensure no two tasks share the same time slot").
    Verify "Copy Snippet" button copies the instruction to the clipboard.
  </verify>
  <done>
    Dashboard provides automated critiques and compensating prompt suggestions for model failures.
  </done>
</task>

</tasks>

<verification>
- [ ] Judge model correctly identifies logical overlaps in task scheduling.
- [ ] Dual-layer scoring (Structural + Logical) is clearly visible.
- [ ] Compensating prompts are actionable and relevant to the errors.
</verification>

<success_criteria>
- Automated logic validation loop is functional.
- Developers can immediately see why a model failed and how to improve the prompt.
- Dashboard provides a comprehensive evaluation of model performance across JSON schema and logical constraints.
</success_criteria>

<output>
After completion, create `.planning/phases/20-llm-optimization-and-evaluation-dashboard/20-03-SUMMARY.md`
</output>
