---
phase: 20-llm-optimization-and-evaluation-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/eval/requirements.txt, backend/eval/golden_cases.json, backend/eval/dashboard.py, backend/eval/.env.example]
autonomous: true
requirements: [EVAL-01, EVAL-02]

must_haves:
  truths:
    - "Streamlit dashboard starts and shows a side-by-side view"
    - "Model outputs are displayed with latency and estimated cost"
    - "Golden dataset cases can be selected from a dropdown"
  artifacts:
    - path: "backend/eval/golden_cases.json"
      provides: "Reference test cases for evaluation"
    - path: "backend/eval/dashboard.py"
      provides: "Main Streamlit application logic"
  key_links:
    - from: "backend/eval/dashboard.py"
      to: "litellm"
      via: "API calls to LLMs"
---

<objective>
Establish an isolated Streamlit-based evaluation environment for side-by-side model comparison.
This plan sets up the basic infrastructure, the golden dataset, and the initial visual interface with latency and cost metrics.
</objective>

<execution_context>
@/Users/eyalatar/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/eyalatar/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-llm-optimization-and-evaluation-dashboard/20-CONTEXT.md
@.planning/phases/20-llm-optimization-and-evaluation-dashboard/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up evaluation environment and Golden Dataset</name>
  <files>backend/eval/requirements.txt, backend/eval/golden_cases.json, backend/eval/.env.example</files>
  <action>
    Create the `backend/eval/` directory and set up the base files:
    - `requirements.txt`: Include `streamlit`, `litellm`, `pandas`, `python-dotenv`.
    - `golden_cases.json`: Define 5-10 initial test scenarios covering various constraints:
      - `scenario_id`: unique ID.
      - `description`: scenario context.
      - `current_time`: reference ISO timestamp.
      - `tasks`: list of academic tasks (title, duration).
      - `constraints`: lectures, gym, sleep windows.
    - `.env.example`: Template for necessary LLM API keys.
  </action>
  <verify>
    Verify files exist and `golden_cases.json` is valid JSON with at least 5 scenarios.
  </verify>
  <done>
    Directory and configuration files created.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build basic side-by-side Streamlit dashboard</name>
  <files>backend/eval/dashboard.py</files>
  <action>
    Implement a Streamlit dashboard with the following features:
    - Sidebar: Dropdown to select a scenario from `golden_cases.json`.
    - Model Selection: Input fields for 'Current Model' (e.g., gpt-4o) and 'Challenger Model' (e.g., gemini/gemini-1.5-flash).
    - Execution logic: Use `litellm.completion` to call both models with the same prompt derived from the selected scenario.
    - Side-by-side columns:
      - Column 1: Current Model output (JSON Tree + Raw Text).
      - Column 2: Challenger Model output (JSON Tree + Raw Text).
    - KPI Metrics: Display Latency (sec) and Cost (USD) for both calls at the top of each column.
    - Determinism: Force `temperature=0` and `response_format={"type": "json_object"}`.
  </action>
  <verify>
    Run `streamlit run backend/eval/dashboard.py` and verify:
    - Can select a scenario.
    - Can input model names.
    - Outputs appear in two columns with latency/cost metrics.
  </verify>
  <done>
    Functional side-by-side comparison dashboard is running.
  </done>
</task>

</tasks>

<verification>
- [ ] `streamlit run backend/eval/dashboard.py` works without errors.
- [ ] Side-by-side view renders JSON from two different models.
- [ ] Latency and cost are calculated and displayed correctly.
</verification>

<success_criteria>
- Streamlit application successfully connects to LLMs via litellm.
- Dashboard allows selecting scenarios and comparing results in real-time.
- Visual JSON representation is implemented (using `st.json`).
</success_criteria>

<output>
After completion, create `.planning/phases/20-llm-optimization-and-evaluation-dashboard/20-01-SUMMARY.md`
</output>
