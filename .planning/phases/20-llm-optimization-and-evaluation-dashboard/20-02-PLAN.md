---
phase: 20-llm-optimization-and-evaluation-dashboard
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified: [backend/eval/dashboard.py, backend/eval/prompts/strategist_default.txt, backend/eval/prompts/scheduler_default.txt]
autonomous: true
requirements: [EVAL-03]

must_haves:
  truths:
    - "System prompt for Challenger model can be edited and immediately re-tested"
    - "Model selection uses litellm dropdown in sidebar"
    - " 'Run All' button executes scenario against entire Golden Dataset"
  artifacts:
    - path: "backend/eval/prompts/strategist_default.txt"
      provides: "Reference prompt for comparison"
  key_links:
    - from: "backend/eval/dashboard.py"
      to: "streamlit.text_area"
      via: "Dynamic prompt update logic"
---

<objective>
Enhance the evaluation dashboard with interactive controls for rapid prompt iteration and batch testing.
This plan focuses on making the environment a true playground where developers can modify system instructions and verify performance across the entire dataset.
</objective>

<execution_context>
@/Users/eyalatar/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/eyalatar/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-llm-optimization-and-evaluation-dashboard/20-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Interactive Playground controls</name>
  <files>backend/eval/dashboard.py, backend/eval/prompts/strategist_default.txt, backend/eval/prompts/scheduler_default.txt</files>
  <action>
    Implement interactive features in `backend/eval/dashboard.py`:
    - Live Prompt Editor: Add `st.text_area` in the sidebar or Challenger column to edit the System Prompt.
    - Model Arena: Replace text inputs with a dropdown selectbox containing common `litellm` model identifiers (gpt-4o, gemini/gemini-1.5-flash, anthropic/claude-3-haiku, etc.).
    - Batch Testing: Add a "Run All" button in the sidebar to execute the Current Model vs. Challenger Model across all scenarios in `golden_cases.json`.
    - Result Storage: Cache or display batch results in a summary table showing pass/fail or performance delta.
    - Default Prompts: Extract current system prompts from backend logic into standalone `.txt` files for initial reference.
  </action>
  <verify>
    Verify "Run All" button triggers calls for all scenarios and displays a summary.
    Verify prompt edits are passed to the `litellm.completion` call for the Challenger model.
  </verify>
  <done>
    Dashboard has interactive prompt editor, model selection, and batch testing capabilities.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Diff Highlighting and Copy Prompt export</name>
  <files>backend/eval/dashboard.py</files>
  <action>
    Enhance the side-by-side view with visual tools:
    - Diff Highlighting: Implement a simple JSON diff logic (using `jsondiff` library if needed, or simple comparison) to highlight differences in task start times or schema mismatches.
    - Export Button: Add a "Copy System Prompt" button to easily copy the current Challenger prompt text to the clipboard.
    - Cost/Token tracking: Display total session cost and per-run token usage in the sidebar.
  </action>
  <verify>
    Verify differences are visually emphasized (e.g., using color or specific text).
    Verify "Copy System Prompt" button correctly copies the text to the clipboard.
  </verify>
  <done>
    Visual comparison and export features are functional.
  </done>
</task>

</tasks>

<verification>
- [ ] Prompt editor correctly updates the input for the Challenger model.
- [ ] "Run All" button correctly iterates through the golden dataset and reports results.
- [ ] "Copy System Prompt" button works as expected.
</verification>

<success_criteria>
- Interactive prompt engineering loop (Edit -> Run -> See results) is established.
- Model arena allows easy benchmarking across different providers via litellm.
- Dashboard provides a summary of batch performance results.
</success_criteria>

<output>
After completion, create `.planning/phases/20-llm-optimization-and-evaluation-dashboard/20-02-SUMMARY.md`
</output>
